# fractal-ai — Claude Code memory entrypoint

Claude Code autoloads `CLAUDE.md`, not `AGENTS.md`. This file exists so the repo's
agent guidance reaches Claude Code sessions too — the actual content lives in
`AGENTS.md` (the cross-tool meta file for agents working **on** this repo) and is
imported below. Keep repo guidance in `AGENTS.md`; this file just re-exports it.

@AGENTS.md
